# netlify.toml
[build]
  publish = "."  # or your build directory if using a build tool

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Customize form settings
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"